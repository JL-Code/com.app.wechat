import 'package:flutter/material.dart';

class ContactsPage extends StatelessWidget {
  static const String routeName = "/contacts";

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: Text("ContactsPage"),
    );
  }
}
